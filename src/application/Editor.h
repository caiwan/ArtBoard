#pragma once 

#include <memory>

namespace ArtBoard
{
	class EditorView;

	class  Editor
	{
	public:
		Editor();

		// void SetView(std::unique_ptr<EditorView> &view) { m_view = view;}

	private:
	// std::unique_ptr<EditorView> m_view;
	};

}
